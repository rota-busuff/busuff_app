name: app_motorista
description: "Aplicativo para rastreamento do motorista."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  shared_preferences: ^2.3.3
  location: ^7.0.1
  http: ^1.3.0
  geolocator: ^13.0.2
  flutter_background_service: ^5.1.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # IMAGENS
  assets:
     - assets/onibus.png
